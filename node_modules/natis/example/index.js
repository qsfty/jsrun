var mybatis = require('../index')

mybatis.config('mapper','config/mapper')
mybatis.config('settings',require('./config/setting'))
mybatis.config('validate',require('./config/validate'))

module.exports = mybatis;

