<?xml version="1.0" encoding="utf-8"?>
<sql id="test">
    <insert id="add" before="unique">
        insert into 
            test(name,password,gender)
        values(
            {{name | q}},
            {{password | q}},
            {{gender | q}}
        )
    </insert>
    
    <select id="unique">
        select
            count(1)
        from
            test
        where
            {{name | eq:'name'}}
            {{id | and:'id','neq'}}
    </select>

    <delete id="delete" before="check">
        delete from
            test
        where
            id = {{id | q}}
    </delete>

    <select id="check">
        select 1
    </select>

    <select id="query" child="name,gender" childName="users" childId="id">
        select
            *
        from
            test
    </select>
</sql>